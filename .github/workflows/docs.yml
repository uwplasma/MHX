name: Docs

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "docs/**"
      - "README.md"
      - "pyproject.toml"
      - ".readthedocs.yaml"
      - "readthedocs.yaml"
      - ".github/workflows/docs.yml"
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "README.md"
      - "pyproject.toml"
      - ".readthedocs.yaml"
      - "readthedocs.yaml"
      - ".github/workflows/docs.yml"

permissions:
  contents: read

jobs:
  sphinx:
    name: Sphinx warnings-as-errors
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2
      - uses: actions/setup-python@v6.2.0
        with:
          python-version: "3.11"
      - name: Install docs dependencies
        run: python -m pip install -e ".[dev,docs]"
      - name: Check docs links and media
        run: python -m pytest tests/test_docs_links.py tests/test_readme_media.py -q
      - name: Build docs
        run: sphinx-build -W -b html docs docs/_build/html
      - name: Upload docs HTML
        uses: actions/upload-artifact@v7.0.1
        with:
          name: mhx-docs-html
          path: docs/_build/html
          if-no-files-found: error
