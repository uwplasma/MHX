[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mhx"
version = "0.2.0"
description = "Reduced MHD / tearing-mode / reconnection model scripts (JAX-based)."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = [
  "jax",
  "jaxlib",
  "numpy",
  "scipy",
  "matplotlib",
  "diffrax",
  "tqdm",
  "typer",
]

[project.scripts]
mhx = "mhx.cli.main:app"

[project.optional-dependencies]
ml = ["optax", "equinox"]
dev = ["pytest", "ruff", "optax", "equinox", "imageio"]

docs = ["sphinx", "sphinx-rtd-theme", "pyyaml"]

[tool.setuptools.packages.find]
where = ["."]
include = ["mhx*"]

[tool.ruff]
line-length = 120
extend-exclude = [
  "scripts/legacy",
  "templates",
  "outputs",
  "docs/_static",
  "bench",
]
