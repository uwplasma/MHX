Scans
=====

Parameter scans over ``(log10_eta, log10_nu)`` compute reachable regions and
support comparisons between grid search and inverse design.

CLI:

.. code-block:: bash

   mhx scan --equilibrium forcefree --grid 4x4

Outputs are written under `outputs/scans/`.
