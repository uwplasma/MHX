# Input, output, and versioning

The IO module reads and writes trajectory NPZ files and run manifests. The
versioning module records the public API version in every artifact.

```{eval-rst}
.. automodule:: mhx.io
   :members:

.. automodule:: mhx.versioning
   :members:
```
